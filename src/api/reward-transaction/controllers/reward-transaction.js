'use strict';

/**
 * reward-transaction controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::reward-transaction.reward-transaction');
