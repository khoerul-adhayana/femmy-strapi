'use strict';

/**
 * reward-transaction router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::reward-transaction.reward-transaction');
